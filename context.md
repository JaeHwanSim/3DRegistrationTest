# 치아 모델 정합 프로그램 개발 컨텍스트

## 현재까지의 작업 요약

### 1. 정합 파이프라인 순서 변경
- 기존: 모델 로드 → PCA 정렬 → FPFH 특징점 추출 → RANSAC → ICP
- 변경: 모델 로드 → FPFH 특징점 추출 → RANSAC → (선택적) PCA → ICP
- 이유: 일반적인 정합 파이프라인에서는 특징점 추출 후 RANSAC으로 전역 정합을 수행함

### 2. UI 흐름 변경
- 버튼 순서와 활성화 순서 변경
- 상태 메시지 및 단계 번호 업데이트
- PCA 단계는 선택적으로 사용 가능하도록 유지

### 3. FPFH 특징점 추출 개선
- 포인트 클라우드 샘플링 밀도 증가 (50,000개 포인트)
- voxel_size 감소 (0.001)로 더 조밀한 다운샘플링
- 특징점 시각화를 위한 구(sphere) 크기 증가 (radius=voxel_size*50)
- 특징점 색상 개선 (소스: 밝은 파란색, 타겟: 밝은 빨간색)
- 원본 모델은 원래 색상 유지 (회색으로 변경하는 코드 제거)

### 4. 중요 매개변수
- sample_density: 50,000 (메쉬 샘플링 포인트 수)
- voxel_size: 0.001 (다운샘플링 크기)
- sphere_radius: voxel_size*50 (특징점 시각화 크기)
- 특징점 선택 기준: 상위 95% (하위 5%만 제외)

### 5. 주요 메소드 설명
- `mesh_to_pointcloud`: 메쉬를 포인트 클라우드로 변환 (50,000개 포인트)
- `execute_fpfh_extraction`: FPFH 특징점 추출 및 시각화
- `execute_ransac`: RANSAC 알고리즘으로 전역 정합
- `execute_pca_alignment`: (선택적) PCA 기반 정렬
- `execute_icp`: ICP 알고리즘으로 미세 정합

## FPFH 특징점과 Point Cloud 차이

### Point Cloud (포인트 클라우드)
- 3D 공간에 있는 단순한 점들의 집합
- (x, y, z) 좌표값만 가짐
- 물체의 표면 형상을 표현
- 모든 점은 동등한 중요도를 가짐

### FPFH 특징점 (Fast Point Feature Histograms)
- 포인트 클라우드에서 추출한 '중요한' 특징 정보
- 각 점의 주변 기하학적 특성을 수치화한 33차원 벡터
- 정합에 필요한 핵심 특징 정보 제공
- 물체의 교두, 홈, 경계선 등 특징적인 부분에 해당

## 다음 단계
- RANSAC 전역 정합 실행 및 테스트
- ICP 미세 정합 수행
- 정합 결과 평가 및 매개변수 최적화
