# 치아 모델 정합 프로그램 개발 컨텍스트

## 현재까지의 작업 요약

### 1. 정합 파이프라인 순서 변경
- 기존: 모델 로드 → PCA 정렬 → FPFH 특징점 추출 → RANSAC → ICP
- 변경: 모델 로드 → FPFH 특징점 추출 → RANSAC → (선택적) PCA → ICP
- 이유: 일반적인 정합 파이프라인에서는 특징점 추출 후 RANSAC으로 전역 정합을 수행함

### 2. UI 흐름 변경
- 버튼 순서와 활성화 순서 변경
- 상태 메시지 및 단계 번호 업데이트
- PCA 단계는 선택적으로 사용 가능하도록 유지

### 3. FPFH 특징점 추출 개선
- 포인트 클라우드 샘플링 밀도 증가 (50,000개 포인트)
- voxel_size 감소 (0.001)로 더 조밀한 다운샘플링
- 특징점 시각화를 위한 구(sphere) 크기 증가 (radius=voxel_size*50)
- 특징점 색상 개선 (소스: 밝은 파란색, 타겟: 밝은 빨간색)
- 원본 모델은 원래 색상 유지 (회색으로 변경하는 코드 제거)

### 4. RANSAC 정합 최적화 시도
- voxel_size 조정 (0.05 → 0.01)을 통한 세밀한 정합 시도
- RANSAC 반복 횟수 증가 (100,000 → 500,000)
- 대응점 거리 임계값 증가 (voxel_size*1.5 → voxel_size*5.0)
- 정합 성공률 향상을 위한 다양한 파라미터 조정

### 5. 정규화 기반 접근법 시도
- 모델 중심 정렬 및 스케일 정규화
- 원래 좌표계로 변환 매커니즘 구현
- fitness 점수는 향상되었으나 시각적 정합 품질에 문제 발생

### 6. 중요 매개변수
- sample_density: 50,000 (메쉬 샘플링 포인트 수)
- voxel_size: 0.001 (다운샘플링 크기)
- sphere_radius: voxel_size*50 (특징점 시각화 크기)
- 특징점 선택 기준: 상위 95% (하위 5%만 제외)
- RANSAC 반복 횟수: 500,000
- 대응점 거리 임계값: voxel_size*5.0

### 7. 주요 메소드 설명
- `mesh_to_pointcloud`: 메쉬를 포인트 클라우드로 변환 (50,000개 포인트)
- `execute_fpfh_extraction`: FPFH 특징점 추출 및 시각화
- `execute_ransac`: RANSAC 알고리즘으로 전역 정합
- `execute_pca_alignment`: (선택적) PCA 기반 정렬
- `execute_icp`: ICP 알고리즘으로 미세 정합
- `preprocess_point_cloud`: 포인트 클라우드 전처리 (중심 정렬 및 정규화)
- `transform_to_original_coordinates`: 정규화된 변환 행렬을 원래 좌표계로 변환

## FPFH 특징점과 Point Cloud 차이

### Point Cloud (포인트 클라우드)
- 3D 공간에 있는 단순한 점들의 집합
- (x, y, z) 좌표값만 가짐
- 물체의 표면 형상을 표현
- 모든 점은 동등한 중요도를 가짐

### FPFH 특징점 (Fast Point Feature Histograms)
- 포인트 클라우드에서 추출한 '중요한' 특징 정보
- 각 점의 주변 기하학적 특성을 수치화한 33차원 벡터
- 정합에 필요한 핵심 특징 정보 제공
- 물체의 교두, 홈, 경계선 등 특징적인 부분에 해당

## 현재 문제점

### 1. 모듈화 부족
- 단일 `registration.py` 파일에 모든 정합 로직이 혼합
- 한 기능 변경 시 다른 기능에 의도치 않은 사이드 이펙트 발생
- 코드 유지보수 및 개발 효율성 저하

### 2. CAD-Scan 정합 도전 과제
- CAD 모델과 3D 스캔 모델의 특성 차이 (완벽한 CAD vs 노이즈 있는 스캔)
- 초기 방향이 완전히 다름
- 부분 일치 문제 (스캔 모델에 추가 영역 존재)

### 3. 정합 성능 문제
- 현재 RANSAC 결과의 fitness 점수 매우 낮음 (0.0001 수준)
- 시각적으로도 정합 품질이 좋지 않음
- 기존 접근법으로는 CAD-Scan 정합에 한계 존재

## 다음 단계

### 1. 모듈화 아키텍처 적용
- 기능별 모듈 분리:
  ```
  src/registration/
  ├── __init__.py
  ├── registration.py         # 메인 인터페이스
  ├── preprocessor.py         # 전처리 모듈
  ├── feature_extractor.py    # 특징 추출 모듈
  ├── global_registration.py  # 전역 정합 모듈
  ├── local_registration.py   # 지역 정합 모듈
  └── utils.py                # 유틸리티 함수
  ```
- 변경 격리를 통한 개발 효율성 향상
- 각 모듈 독립적 테스트 가능

### 2. CAD-to-Scan 특화 정합 전략 구현
- PCA 기반 방향 초기화
- 다양한 방향 시도 (주요 축 회전)
- 점대점 → 점대면 ICP 단계적 적용
- 부분 일치를 고려한 알고리즘 적용

### 3. 정합 품질 개선
- 특징 추출 매개변수 최적화
- 다양한 초기 변환 시도
- 정합 전/후 시각화 도구 개선
- 정량적 평가 메트릭 구현

### 4. 사용자 경험 개선
- 정합 진행 상황 시각화
- 다양한 정합 옵션 UI 추가
- 결과 품질 평가 피드백 제공
